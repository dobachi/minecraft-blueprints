# minecraft-blueprints（AI 向け作業指針）

Minecraft Bedrock の建築定義を相対座標で蓄積するリポジトリ。
実行環境は `minecraft-claude`（MCP サーバ）が提供する。ここは「何を建てるか」だけを持つ。

**Minecraft を操作する前に `projects/minecraft-claude/CLAUDE.md` の行動原則を読むこと。**
座標系・往復数の最小化・足元を埋めない、といった前提はそちらにある。

## 建てるときの手順

1. **現状を測る** — `player get_location` で現在地、`blocks get_top_solid_block` で
   予定地の地表。**`y` は渡さない**（渡すとその高度から下を探すので、洞窟の底が返る）
2. **計画を出す** — `node scripts/build-plan.js <blueprint> --at <x> <y> <z>`
3. **計画をユーザに見せる** — 何をどこに置くか、既存ブロックを壊すかを明示してから着手する
4. **実行する** — `--format json` の各 op を対応する MCP ツールで順に呼ぶ
5. **記録する** — 新しく踏んだ罠は `docs/PITFALLS.md` と当該ステップの `why:` に書く

## 新しいブループリントを書くとき

- **絶対座標を書かない。** 一箇所でも書いた時点で再現性が失われる
- `anchor.description` を必ず書く。原点がどこを指すか曖昧だと他人（と将来の自分）が使えない
- **敷地クリアを最初の step にする。** `hollow` は内部の既存ブロックを掃除しないので、
  木や草の中に建てると室内に残留物が出る
- 罠を踏んだら `why:` に書く。このコレクションの価値は形より知識の蓄積にある
- 未解決の課題は `todo:` に残す。黙って放置しない

## やってはいけないこと

- **スクリプトから直接 WebSocket に繋ぐ。** ポート 8001 は Claude Code が起動した
  MCP サーバが握っている。計画の生成（このリポジトリ）と実行（MCP）は分離を保つ
- **他人の建造物の上に建てる。** マルチプレイでは持ち主の許可を取る。`/fill` による
  上書きは元のブロック配置を記録していない限り復元できない
- **`build-plan.js` に実行機能を足す。** 生成と実行を分けているのは、着手前に必ず
  人間が計画を確認できるようにするため

## 実行前の確認

ユーザに提示すべき情報:

- 建つ場所（絶対座標）とプレイヤーからの距離・方角
- 破壊される既存ブロックの有無（`get_top_solid_block` で下調べした結果）
- 総ブロック数と操作回数

`bounds` を外れる step があると `build-plan.js` が警告を出す。警告は無視せず、
step か `bounds` のどちらが誤っているかを判断すること。
